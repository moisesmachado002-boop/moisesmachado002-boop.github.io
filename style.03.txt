: grid; grid-template-columns: 1fr 1fr; }
  .filter-row input:first-child { grid-column: 1 / -1; }
  .filter-row select, .filter-row input[type="month"] { max-width: none; }
  .settings-panel { align-items: flex-start; flex-direction: column; }
  .bottom-nav { overflow-x: auto; grid-template-columns: repeat(7, minmax(70px, 1fr)); justify-content: start; }
}

@media (max-width: 520px) {
  .toolbar { align-items: end; }
  .hero-card { padding: 20px; }
  .hero-card strong { font-size: 2.15rem; }
  .form-grid { grid-template-columns: 1fr; }
  .field.full { grid-column: auto; }
  .section-title { align-items: flex-start; }
  .section-title .action-pair { display: grid; }
  .panel { padding: 15px; }
  .list-item { grid-template-columns: minmax(0, 1fr) auto; }
}


/* V1.2 — disponibilidade por local do dinheiro */
.panel-subtitle { color: var(--muted); font-size: .82rem; margin: 4px 0 0; }
.availability-grid .stat-card strong { font-size: 1.18rem; }
.balance-settings-form { display: grid; grid-template-columns: minmax(0,1fr) minmax(0,1fr) auto; gap: 12px; align-items: end; }
.balance-settings-form .primary { min-height: 44px; }
@media (max-width: 680px) {
  .balance-settings-form { grid-template-columns: 1fr 1fr; }
  .balance-settings-form .primary { grid-column: 1 / -1; }
}
@media (max-width: 430px) {
  .balance-settings-form { grid-template-columns: 1fr; }
  .balance-settings-form .primary { grid-column: auto; }
}


/* V1.3 — Login e sincronização Supabase */
.auth-hidden { display: none !important; }
.auth-gate {
  min-height: 100vh; min-height: 100dvh; padding: 24px; display: grid; place-items: center;
  background: radial-gradient(circle at 20% 10%, color-mix(in srgb, var(--brand) 18%, transparent), transparent 38%), var(--bg);
}
.auth-gate.hidden { display: none; }
.auth-card {
  width: min(460px, 100%); background: var(--surface); border: 1px solid var(--line); border-radius: 26px;
  padding: 28px; box-shadow: 0 28px 80px rgba(15, 23, 42, .16);
}
.auth-logo { width: 58px; height: 58px; display: grid; place-items: center; border-radius: 18px; background: var(--brand); color: #fff; font-weight: 950; font-size: 1.2rem; margin-bottom: 18px; }
.auth-card h1 { font-size: 1.8rem; margin-bottom: 8px; }
.auth-subtitle { color: var(--muted); line-height: 1.5; margin-bottom: 20px; }
.auth-form { display: grid; gap: 12px; }
.auth-primary { width: 100%; min-height: 46px; }
.auth-message { min-height: 22px; margin-top: 14px; color: var(--muted); font-size: .84rem; line-height: 1.4; }
.auth-message.error { color: var(--danger); }
.auth-message.success { color: var(--success); }
.auth-security { margin: 14px 0 0; padding-top: 14px; border-top: 1px solid var(--line); color: var(--muted); font-size: .78rem; }
.top-actions { display: flex; align-items: center; gap: 8px; }
.logout-top { padding: 8px 6px; }
.sync-pill { display: flex; align-items: center; gap: 7px; padding: 7px 10px; border-radius: 999px; border: 1px solid var(--line); background: var(--surface); color: var(--muted); font-size: .72rem; font-weight: 800; white-space: nowrap; }
.sync-dot { width: 8px; height: 8px; border-radius: 50%; background: var(--muted); box-shadow: 0 0 0 3px color-mix(in srgb, var(--muted) 15%, transparent); }
.sync-pill.synced .sync-dot { background: var(--success); box-shadow: 0 0 0 3px color-mix(in srgb, var(--success) 15%, transparent); }
.sync-pill.syncing .sync-dot { background: var(--warning); animation: syncPulse 1s infinite alternate; }
.sync-pill.offline .sync-dot, .sync-pill.error .sync-dot { background: var(--danger); }
@keyframes syncPulse { to { transform: scale(1.5); opacity: .55; } }
.cloud-account-row { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin: 8px 0 14px; }
.cloud-account-row > div { padding: 13px; border: 1px solid var(--line); border-radius: 14px; background: var(--surface-2); min-width: 0; }
.cloud-label { display: block; color: var(--muted); font-size: .72rem; margin-bottom: 5px; }
.cloud-account-row strong { display: block; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-size: .86rem; }
.cloud-actions { margin-top: 10px; }
@media (max-width: 640px) {
  .auth-gate { padding: 14px; }
  .auth-card { padding: 22px 18px; }
  .sync-pill span:last-child { display: none; }
  .sync-pill { padding: 9px; }
  .logout-top { display: none; }
  .cloud-account-row { grid-template-columns: 1fr; }
}
