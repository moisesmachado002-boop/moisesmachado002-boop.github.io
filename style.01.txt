:root {
  --bg: #f5f7fb;
  --surface: #ffffff;
  --surface-2: #eef2f7;
  --text: #172033;
  --muted: #6b7280;
  --line: #dfe5ee;
  --brand: #2563eb;
  --brand-strong: #1d4ed8;
  --success: #14804a;
  --danger: #d13b3b;
  --warning: #b86b00;
  --shadow: 0 12px 30px rgba(15, 23, 42, .08);
  --radius: 20px;
}

:root[data-theme="dark"] {
  --bg: #0b1120;
  --surface: #111827;
  --surface-2: #1c2535;
  --text: #f8fafc;
  --muted: #9ca3af;
  --line: #263244;
  --brand: #60a5fa;
  --brand-strong: #3b82f6;
  --success: #4ade80;
  --danger: #fb7185;
  --warning: #fbbf24;
  --shadow: 0 12px 30px rgba(0, 0, 0, .22);
}

* { box-sizing: border-box; }
html { background: var(--bg); }
body {
  margin: 0;
  min-height: 100vh;
  background: var(--bg);
  color: var(--text);
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}
button, input, select, textarea { font: inherit; }
button { cursor: pointer; }
.app-shell { width: min(1180px, 100%); margin: 0 auto; padding: 0 18px 110px; }
.topbar {
  display: flex; align-items: center; justify-content: space-between;
  padding: 24px 0 14px; position: sticky; top: 0; z-index: 20;
  background: color-mix(in srgb, var(--bg) 90%, transparent); backdrop-filter: blur(16px);
}
h1, h2, h3, p { margin-top: 0; }
h1 { margin-bottom: 0; font-size: clamp(1.5rem, 5vw, 2rem); }
h2 { margin-bottom: 0; font-size: 1.16rem; }
h3 { margin-bottom: 6px; }
.eyebrow { color: var(--brand); font-size: .72rem; font-weight: 800; letter-spacing: .13em; margin-bottom: 4px; }
.icon-btn { width: 42px; height: 42px; border-radius: 14px; border: 1px solid var(--line); background: var(--surface); color: var(--text); font-size: 1.25rem; }
.view { display: none; animation: fade .18s ease; }
.view.active { display: block; }
@keyframes fade { from { opacity: .5; transform: translateY(4px); } }
.toolbar, .section-title, .panel-head, .filter-row, .action-pair { display: flex; align-items: center; gap: 12px; }
.toolbar, .section-title { justify-content: space-between; margin: 14px 0 18px; }
.action-pair { flex-wrap: nowrap; }
.wrap { flex-wrap: wrap; }
.field { display: grid; gap: 6px; }
.field span { font-size: .78rem; color: var(--muted); font-weight: 700; }
.field.compact { min-width: 155px; }
input, select, textarea {
  width: 100%; border: 1px solid var(--line); background: var(--surface); color: var(--text);
  border-radius: 12px; padding: 12px 13px; outline: none;
}
input:focus, select:focus, textarea:focus { border-color: var(--brand); box-shadow: 0 0 0 3px color-mix(in srgb, var(--brand) 16%, transparent); }
.segmented { display: flex; padding: 4px; border-radius: 13px; background: var(--surface-2); }
.segmented button { border: 0; background: transparent; color: var(--muted); padding: 9px 13px; border-radius: 10px; font-weight: 750; }
.segmented button.active { background: var(--surface); color: var(--text); box-shadow: 0 2px 10px rgba(15,23,42,.08); }
.hero-card {
  display: flex; justify-content: space-between; align-items: center; padding: 24px;
  background: linear-gradient(135deg, #0f172a 0%, #1e3a8a 100%); color: white;
  border-radius: 24px; box-shadow: var(--shadow); margin-bottom: 16px;
}
.hero-card span, .hero-card small { display: block; color: #cbd5e1; }
.hero-card strong { display: block; font-size: clamp(2rem, 9vw, 3rem); margin: 5px 0; letter-spacing: -.04em; }
.hero-indicator { width: 52px; height: 52px; border-radius: 18px; display: grid; place-items: center; background: rgba(255,255,255,.12); font-size: 1.5rem; }
.stats-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; }
.stat-card, .panel, .goal-card, .credit-card {
  background: var(--surface); border: 1px solid var(--line); border-radius: var(--radius); box-shadow: var(--shadow);
}
.stat-card { padding: 16px; }
.stat-card span, .stat-card small { display: block; color: var(--muted); }
.stat-card strong { display: block; font-size: 1.25rem; margin: 6px 0 2px; }
.stat-card.positive strong { color: var(--success); }
.stat-card.negative strong { color: var(--danger); }
.stat-card.warning strong { color: var(--warning); }
.panel { padding: 18px; margin-top: 14px; }
.panel-head { justify-content: space-between; margin-bottom: 14px; }
.primary, .secondary, .danger, .text-btn, .file-label {
  border-radius: 12px; padding: 11px 14px; font-weight: 800; border: 0; text-decoration: none;
}
.primary { background: var(--brand); color: #fff; }
.primary:hover { background: var(--brand-strong); }
.primary.small { padding: 9px 12px; }
.secondary, .file-label { background: var(--surface-2); color: var(--text); border: 1px solid var(--line); }
.danger { background: color-mix(in srgb, var(--danger) 12%, var(--surface)); color: var(--danger); border: 1px solid color-mix(in srgb, var(--danger) 30%, var(--line)); }
.text-btn { background: transparent; color: var(--brand); padding-inline: 2px; }
.file-label input { display: none; }
.list { display: grid; gap: 8px; }
.list-item {
  display: grid; grid-template-columns: minmax(0, 1fr) auto; gap: 12px; align-items: center;
  padding: 13px 14px; border-radius: 15px; background: var(--surface-2); border: 1px solid transparent;
}
.list-item:hover { border-color: var(--line); }
.item-main { min-width: 0; }
.item-title { display: flex; align-items: center; gap: 8px; font-weight: 800; }
.item-title span:first-child { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.item-meta { color: var(--muted); font-size: .78rem; margin