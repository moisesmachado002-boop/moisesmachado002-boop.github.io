/* Etapa 12 — fundo escuro sofisticado (revisão 2)
   SOMENTE APARÊNCIA. Agora o fundo escuro faz parte do próprio portal. */

:root{
  --ambient-navy:#06101e;
  --ambient-navy-2:#0a1729;
  --ambient-slate:#111f34;
  --ambient-blue:#2563eb;
  --ambient-cyan:#0ea5e9;
  --ambient-violet:#6366f1;
}

html{
  min-height:100%;
  background:#040b14;
}

body{
  position:relative;
  isolation:isolate;
  min-height:100vh;
  background:
    radial-gradient(circle at 10% 7%,rgba(37,99,235,.30),transparent 27rem),
    radial-gradient(circle at 91% 15%,rgba(14,165,233,.21),transparent 24rem),
    radial-gradient(circle at 72% 92%,rgba(99,102,241,.16),transparent 29rem),
    linear-gradient(145deg,#030a13 0%,#071322 45%,#0d1b30 100%);
  background-attachment:fixed;
  overflow-x:hidden;
}

body::before{
  content:"";
  position:fixed;
  inset:0;
  z-index:-2;
  pointer-events:none;
  opacity:.38;
  background-image:
    linear-gradient(rgba(255,255,255,.028) 1px,transparent 1px),
    linear-gradient(90deg,rgba(255,255,255,.028) 1px,transparent 1px);
  background-size:42px 42px;
  -webkit-mask-image:linear-gradient(to bottom,rgba(0,0,0,.9),transparent 96%);
  mask-image:linear-gradient(to bottom,rgba(0,0,0,.9),transparent 96%);
}

body::after{
  content:"";
  position:fixed;
  z-index:-1;
  pointer-events:none;
  width:min(56vw,720px);
  aspect-ratio:1;
  right:-20vw;
  top:28vh;
  border-radius:50%;
  background:radial-gradient(circle,rgba(14,165,233,.15) 0%,rgba(37,99,235,.06) 40%,transparent 72%);
  filter:blur(8px);
}

/* O painel principal agora também é escuro — a mudança fica visível dentro do portal. */
.container{
  position:relative;
  z-index:1;
  padding:30px;
  background:
    radial-gradient(circle at 100% 0%,rgba(37,99,235,.16),transparent 25rem),
    radial-gradient(circle at 0% 65%,rgba(14,165,233,.08),transparent 23rem),
    linear-gradient(155deg,rgba(7,17,31,.97) 0%,rgba(10,24,43,.97) 55%,rgba(15,31,53,.97) 100%);
  border:1px solid rgba(148,163,184,.22);
  box-shadow:
    0 36px 95px rgba(0,0,0,.46),
    0 10px 32px rgba(2,8,23,.34),
    inset 0 1px 0 rgba(255,255,255,.07);
  backdrop-filter:blur(15px);
  -webkit-backdrop-filter:blur(15px);
}

.container::before{
  content:"";
  position:absolute;
  inset:0;
  z-index:-1;
  pointer-events:none;
  border-radius:inherit;
  background:
    linear-gradient(135deg,rgba(255,255,255,.035),transparent 23%),
    linear-gradient(315deg,rgba(37,99,235,.05),transparent 30%);
}

.container::after{
  content:"";
  position:absolute;
  top:-1px;
  left:7%;
  right:7%;
  height:2px;
  z-index:2;
  pointer-events:none;
  border-radius:999px;
  background:linear-gradient(90deg,transparent,#2563eb,#22d3ee,#6366f1,transparent);
  box-shadow:0 0 26px rgba(37,99,235,.55);
}

/* Título principal */
.container > h2{
  color:#f8fafc;
  text-shadow:0 3px 18px rgba(0,0,0,.34);
}

/* Áreas de conteúdo continuam claras e parecem cartões sobre o fundo escuro. */
.form-grid,
.totais-dashboard,
.filter-bar,
.table-container,
.catalog-panel,
.invoice-payment-section,
.cloud-panel,
.proximas-faturas-section{
  background:rgba(255,255,255,.97);
  border-color:rgba(203,213,225,.92);
  box-shadow:
    0 14px 34px rgba(0,0,0,.18),
    0 4px 12px rgba(2,8,23,.10),
    inset 0 1px 0 rgba(255,255,255,.9);
}

/* Espaço entre os blocos deixa o fundo aparecer de verdade. */
.form-grid{margin-bottom:18px}
.totais-dashboard{margin-bottom:18px}
.filter-bar{margin-bottom:18px}
.table-container{margin-bottom:22px}

/* Navegação */
.family-tabs-nav{
  background:rgba(238,244,252,.97);
  border-color:rgba(191,203,220,.92);
  box-shadow:
    0 12px 28px rgba(0,0,0,.20),
    inset 0 1px 0 rgba(255,255,255,.94);
}

.family-tab-helper{
  background:linear-gradient(135deg,rgba(239,246,255,.98),rgba(248,250,252,.98));
  box-shadow:0 8px 22px rgba(0,0,0,.12);
}

/* Painéis extras gerados pelo JS */
.catalog-panel,
.invoice-payment-section,
.cloud-panel,
.proximas-faturas-section{
  margin-top:16px;
  margin-bottom:16px;
}

/* Botões do rodapé ficam mais integrados ao fundo. */
.actions-footer{
  padding-top:4px;
}
.actions-footer .btn-secondary,
.actions-footer .file-upload-label{
  background:rgba(255,255,255,.92);
  border-color:rgba(203,213,225,.9);
}

@media(max-width:720px){
  body{
    padding:8px 6px 22px;
    background:
      radial-gradient(circle at 8% 4%,rgba(37,99,235,.27),transparent 16rem),
      radial-gradient(circle at 94% 18%,rgba(14,165,233,.18),transparent 15rem),
      linear-gradient(155deg,#030a13 0%,#071322 48%,#0d1c31 100%);
    background-attachment:scroll;
  }

  body::before{
    background-size:32px 32px;
    opacity:.27;
  }

  body::after{
    width:320px;
    right:-170px;
    top:36vh;
  }

  .container{
    margin:6px auto;
    padding:15px;
    border-radius:18px;
    background:
      radial-gradient(circle at 100% 0%,rgba(37,99,235,.15),transparent 17rem),
      linear-gradient(155deg,rgba(6,16,30,.98),rgba(10,24,43,.98) 58%,rgba(15,31,53,.98));
    box-shadow:
      0 24px 58px rgba(0,0,0,.43),
      0 5px 18px rgba(2,8,23,.28);
  }

  .container > h2{
    margin-top:4px;
    margin-bottom:18px;
    color:#fff;
  }

  .form-grid,
  .totais-dashboard,
  .filter-bar,
  .table-container,
  .catalog-panel,
  .invoice-payment-section,
  .cloud-panel,
  .proximas-faturas-section{
    background:#fff;
    box-shadow:0 10px 24px rgba(0,0,0,.18);
  }
}

@media(max-width:430px){
  .container{padding:12px;border-radius:16px}
  .container::after{left:13%;right:13%}
}

@media print{
  html,body{background:#fff!important}
  body::before,body::after,.container::before,.container::after{display:none!important}
  .container{background:#fff!important;box-shadow:none!important;border:0!important}
  .container > h2{color:#0f172a!important;text-shadow:none!important}
}
