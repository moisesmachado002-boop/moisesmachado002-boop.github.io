/* Etapa 12 — fundo escuro sofisticado
   SOMENTE APARÊNCIA. Mantém cards e conteúdo claros para preservar leitura. */

:root{
  --ambient-navy:#07111f;
  --ambient-navy-2:#0b1729;
  --ambient-slate:#111d31;
  --ambient-blue:#2563eb;
  --ambient-cyan:#0ea5e9;
  --ambient-violet:#6366f1;
}

html{
  min-height:100%;
  background:var(--ambient-navy);
}

body{
  position:relative;
  isolation:isolate;
  min-height:100vh;
  background:
    radial-gradient(circle at 12% 8%,rgba(37,99,235,.25),transparent 25rem),
    radial-gradient(circle at 88% 16%,rgba(14,165,233,.18),transparent 23rem),
    radial-gradient(circle at 76% 90%,rgba(99,102,241,.14),transparent 28rem),
    linear-gradient(145deg,var(--ambient-navy) 0%,var(--ambient-navy-2) 46%,var(--ambient-slate) 100%);
  background-attachment:fixed;
  overflow-x:hidden;
}

/* Luz ambiente e textura discreta */
body::before{
  content:"";
  position:fixed;
  inset:0;
  z-index:-2;
  pointer-events:none;
  opacity:.32;
  background-image:
    linear-gradient(rgba(255,255,255,.025) 1px,transparent 1px),
    linear-gradient(90deg,rgba(255,255,255,.025) 1px,transparent 1px);
  background-size:42px 42px;
  -webkit-mask-image:linear-gradient(to bottom,rgba(0,0,0,.82),transparent 92%);
  mask-image:linear-gradient(to bottom,rgba(0,0,0,.82),transparent 92%);
}

body::after{
  content:"";
  position:fixed;
  z-index:-1;
  pointer-events:none;
  width:min(54vw,680px);
  aspect-ratio:1;
  right:-18vw;
  top:30vh;
  border-radius:50%;
  background:radial-gradient(circle,rgba(14,165,233,.12) 0%,rgba(37,99,235,.05) 38%,transparent 70%);
  filter:blur(10px);
}

/* Painel principal flutuando sobre o fundo escuro */
.container{
  position:relative;
  z-index:1;
  background:rgba(255,255,255,.955);
  border:1px solid rgba(255,255,255,.52);
  box-shadow:
    0 34px 90px rgba(0,0,0,.38),
    0 8px 28px rgba(2,8,23,.24),
    inset 0 1px 0 rgba(255,255,255,.96);
  backdrop-filter:blur(14px);
  -webkit-backdrop-filter:blur(14px);
}

.container::before{
  content:"";
  position:absolute;
  inset:0;
  z-index:-1;
  pointer-events:none;
  border-radius:inherit;
  background:
    linear-gradient(135deg,rgba(255,255,255,.72),transparent 22%),
    radial-gradient(circle at 100% 0%,rgba(37,99,235,.055),transparent 22rem);
}

/* Pequena linha luminosa no topo do painel */
.container::after{
  content:"";
  position:absolute;
  top:-1px;
  left:6%;
  right:6%;
  height:2px;
  z-index:2;
  pointer-events:none;
  border-radius:999px;
  background:linear-gradient(90deg,transparent,rgba(37,99,235,.78),rgba(14,165,233,.72),transparent);
  box-shadow:0 0 20px rgba(37,99,235,.34);
}

/* Seções ficam um pouco mais nítidas contra o fundo */
.form-grid,
.totais-dashboard,
.table-container,
.catalog-panel,
.invoice-payment-section,
.cloud-panel,
.proximas-faturas-section{
  border-color:#d7e1ec;
  box-shadow:
    0 10px 26px rgba(15,23,42,.065),
    inset 0 1px 0 rgba(255,255,255,.86);
}

/* Navegação recebe um leve acabamento de vidro */
.family-tabs-nav{
  background:rgba(241,245,249,.94);
  border-color:rgba(203,213,225,.9);
  box-shadow:
    0 8px 22px rgba(15,23,42,.08),
    inset 0 1px 0 rgba(255,255,255,.9);
}

/* Dá mais presença ao título sem mudar a identidade */
h2{
  text-shadow:0 1px 0 rgba(255,255,255,.7);
}

@media(max-width:720px){
  body{
    padding:10px 8px 24px;
    background:
      radial-gradient(circle at 12% 5%,rgba(37,99,235,.23),transparent 15rem),
      radial-gradient(circle at 92% 19%,rgba(14,165,233,.15),transparent 14rem),
      linear-gradient(155deg,#06101e 0%,#0a1628 52%,#101d31 100%);
    background-attachment:scroll;
  }

  body::before{
    background-size:34px 34px;
    opacity:.24;
  }

  body::after{
    width:310px;
    right:-155px;
    top:38vh;
    opacity:.8;
  }

  .container{
    background:rgba(255,255,255,.97);
    border-color:rgba(255,255,255,.46);
    box-shadow:
      0 22px 52px rgba(0,0,0,.34),
      0 6px 18px rgba(2,8,23,.20);
    backdrop-filter:blur(8px);
    -webkit-backdrop-filter:blur(8px);
  }

  .container::after{
    left:12%;
    right:12%;
  }
}

@media(max-width:430px){
  body::before{background-size:30px 30px}
  .container{border-radius:17px}
}

@media print{
  html,body{background:#fff!important}
  body::before,body::after,.container::before,.container::after{display:none!important}
  .container{background:#fff!important;box-shadow:none!important;border:0!important}
}
