/* Etapa 10 — refinamento visual do portal
   SOMENTE APARÊNCIA: nenhuma regra de negócio, cálculo ou comportamento é alterado. */

:root{
  --ui-bg:#eef3f8;
  --ui-surface:#ffffff;
  --ui-surface-soft:#f8fafc;
  --ui-line:#dbe4ee;
  --ui-shadow:0 18px 45px rgba(15,23,42,.08);
  --ui-shadow-soft:0 8px 22px rgba(15,23,42,.055);
  --ui-radius-lg:18px;
  --ui-radius-md:13px;
  --ui-radius-sm:10px;
}

body{
  min-height:100vh;
  background:
    radial-gradient(circle at 8% 0%,rgba(37,99,235,.11),transparent 31rem),
    radial-gradient(circle at 100% 18%,rgba(14,165,233,.07),transparent 27rem),
    linear-gradient(180deg,#f8fafc 0%,var(--ui-bg) 100%);
  color:#0f172a;
}

.container{
  max-width:1100px;
  border:1px solid rgba(203,213,225,.78);
  border-radius:22px;
  box-shadow:0 24px 70px rgba(15,23,42,.10);
}

h2{
  letter-spacing:-.03em;
  font-size:clamp(1.5rem,3vw,2rem);
}

/* Navegação */
.family-tabs-nav{
  gap:7px;
  padding:7px;
  border-color:#dbe4ee;
  border-radius:14px;
  background:rgba(241,245,249,.92);
  box-shadow:inset 0 1px 0 rgba(255,255,255,.85);
}

.family-tab-btn{
  min-height:44px;
  border-radius:10px;
  transition:background-color .18s ease,color .18s ease,box-shadow .18s ease,transform .15s ease;
}

.family-tab-btn:hover{transform:translateY(-1px)}
.family-tab-btn.active{
  background:linear-gradient(135deg,#2563eb,#1d4ed8);
  box-shadow:0 8px 18px rgba(37,99,235,.20);
}

.family-tab-helper{
  padding:13px 15px;
  border-radius:12px;
  border-color:#bfdbfe;
  background:linear-gradient(135deg,#eff6ff,#f8fbff);
}

/* Formulários e áreas principais */
.form-grid,
.catalog-panel,
.invoice-payment-section,
.cloud-panel,
.proximas-faturas-section{
  border-color:var(--ui-line);
  border-radius:var(--ui-radius-lg);
  box-shadow:var(--ui-shadow-soft);
}

.form-grid{
  padding:20px;
  background:linear-gradient(180deg,#fff 0%,#fbfdff 100%);
}

label{letter-spacing:.035em}

input,select{
  min-height:43px;
  border-radius:10px;
  border-color:#cbd5e1;
  transition:border-color .18s ease,box-shadow .18s ease,background-color .18s ease;
}

input:hover,select:hover{border-color:#94a3b8}
input:focus,select:focus{
  border-color:#2563eb;
  box-shadow:0 0 0 4px rgba(37,99,235,.10);
}

button,.file-upload-label{
  border-radius:10px;
  transition:transform .15s ease,box-shadow .15s ease,filter .15s ease,background-color .18s ease;
}
button:hover,.file-upload-label:hover{transform:translateY(-1px)}
button:active,.file-upload-label:active{transform:translateY(0)}

.btn-add{
  background:linear-gradient(135deg,#2563eb,#1d4ed8);
  box-shadow:0 8px 18px rgba(37,99,235,.18);
}
.btn-pdf{
  background:linear-gradient(135deg,#16a34a,#15803d);
  box-shadow:0 8px 18px rgba(22,163,74,.16);
}
.btn-secondary{background:#f8fafc}

/* Totais */
.totais-dashboard{
  padding:17px;
  gap:13px;
  border-radius:16px;
  border-color:var(--ui-line);
  background:rgba(248,250,252,.92);
}

.card-total{
  min-height:76px;
  padding:14px 15px;
  border:1px solid #e2e8f0;
  border-left:4px solid #2563eb;
  border-radius:12px;
  box-shadow:0 7px 17px rgba(15,23,42,.055);
}
.card-total.geral{
  background:linear-gradient(135deg,#ecfdf5,#f0fdf4);
  border-color:#bbf7d0;
  border-left-color:#16a34a;
}
.card-total-value{font-size:1.08rem;margin-top:6px}

/* Faturas */
.invoice-payment-section{
  padding:18px;
  background:linear-gradient(180deg,#fff 0%,#f9fbfd 100%);
}

.invoice-payment-summary{gap:10px}
.invoice-payment-summary>div{
  padding:12px 13px;
  border-radius:11px;
  border-color:#e2e8f0;
  box-shadow:0 5px 14px rgba(15,23,42,.04);
}

.invoice-payment-grid{gap:13px}

.invoice-payment-card{
  position:relative;
  overflow:hidden;
  padding:14px;
  border:1px solid #e2e8f0;
  border-left:5px solid #94a3b8;
  border-radius:14px;
  box-shadow:0 9px 24px rgba(15,23,42,.07);
  transition:transform .16s ease,box-shadow .16s ease,border-color .16s ease;
}

.invoice-payment-card:hover{
  transform:translateY(-2px);
  box-shadow:0 14px 30px rgba(15,23,42,.10);
}

.invoice-payment-card::after{
  content:"";
  position:absolute;
  top:-42px;
  right:-42px;
  width:105px;
  height:105px;
  border-radius:50%;
  background:rgba(148,163,184,.06);
  pointer-events:none;
}

.invoice-payment-card.status-aberta{border-left-color:#dc2626}
.invoice-payment-card.status-aberta::after{background:rgba(220,38,38,.055)}
.invoice-payment-card.status-parcial{border-left-color:#d97706}
.invoice-payment-card.status-parcial::after{background:rgba(217,119,6,.065)}
.invoice-payment-card.status-paga{border-left-color:#16a34a}
.invoice-payment-card.status-paga::after{background:rgba(22,163,74,.06)}
.person-invoice-card{border-left-color:#4f46e5}
.person-invoice-card::after{background:rgba(79,70,229,.055)}

.invoice-payment-card-head{margin-bottom:12px}
.invoice-payment-card-head>strong{font-size:14px;letter-spacing:-.01em}

.invoice-status-badge,.invoice-person-badge{
  padding:5px 8px;
  border:1px solid rgba(148,163,184,.18);
  box-shadow:inset 0 1px 0 rgba(255,255,255,.55);
}

.invoice-payment-values{
  gap:8px;
  margin-bottom:12px;
  padding:10px;
  border-radius:10px;
  background:#f8fafc;
  border:1px solid #eef2f7;
}
.invoice-payment-values strong{font-size:13.5px}

.invoice-payment-actions{gap:6px}
.invoice-payment-actions button{
  min-height:34px;
  border-radius:8px;
  font-weight:700;
}

/* Cartões / limites */
.card-credit-box{
  padding:11px;
  border-radius:10px;
  border-color:#dbeafe;
  background:linear-gradient(180deg,#fbfdff,#f6faff);
}
.card-credit-progress{
  height:7px;
  background:#e5eaf1;
}
.card-credit-progress span{
  background:linear-gradient(90deg,#2563eb,#0ea5e9);
}

.catalog-panel{
  padding:16px;
  background:linear-gradient(180deg,#fff,#f8fafc);
}
.catalog-item{
  padding:12px;
  border-radius:11px;
  box-shadow:0 4px 12px rgba(15,23,42,.035);
}
.catalog-edit,.catalog-delete,.catalog-config{border-radius:8px}
.catalog-card-details{font-weight:700}

/* Próximas faturas */
.proximas-faturas-section{
  padding:18px;
  background:linear-gradient(180deg,#fff,#f8fafc);
}
.proximas-faturas-grid{gap:12px}
.fatura-futura-card{
  position:relative;
  overflow:hidden;
  min-height:80px;
  padding:13px 14px;
  border-radius:12px;
  border-color:#e2e8f0;
  box-shadow:0 7px 18px rgba(15,23,42,.055);
}
.fatura-futura-card::after{
  content:"";
  position:absolute;
  width:64px;
  height:64px;
  right:-27px;
  top:-27px;
  border-radius:50%;
  background:rgba(217,119,6,.07);
}
.fatura-futura-valor{font-size:1.08rem;margin-top:6px}

/* Filtros e histórico */
.filter-bar{
  padding:13px;
  gap:10px;
  border:1px solid var(--ui-line);
  border-radius:13px;
  background:#f8fafc;
}
.table-container{
  border-radius:14px;
  border-color:var(--ui-line);
  background:#fff;
  box-shadow:0 7px 20px rgba(15,23,42,.045);
}
th{
  letter-spacing:.035em;
  background:#f1f5f9;
}
th,td{padding:11px 12px}
tbody tr{transition:background-color .14s ease}
tbody tr:hover{background:#f8fafc}

/* Modais */
.catalog-modal-backdrop,.card-settings-backdrop{backdrop-filter:blur(3px)}
.catalog-modal,.card-settings-modal{
  border:1px solid rgba(203,213,225,.75);
  border-radius:16px;
  box-shadow:0 26px 70px rgba(15,23,42,.24);
}
.catalog-close{border-radius:10px}

/* Nuvem */
.cloud-panel{
  padding:17px;
  background:linear-gradient(180deg,#fff,#f8fafc);
}
.cloud-status{border:1px solid rgba(148,163,184,.22)}

/* Rodapé de ações */
.actions-footer{gap:12px}
.backup-buttons{flex-wrap:wrap}

@media(max-width:720px){
  body{padding:10px 8px 24px}
  .container{margin:8px auto;padding:15px;border-radius:16px}
  .family-tabs-nav{position:sticky;top:6px;z-index:20;box-shadow:0 8px 22px rgba(15,23,42,.08)}
  .form-grid,.invoice-payment-section,.cloud-panel,.proximas-faturas-section{padding:14px;border-radius:13px}
  .invoice-payment-card{padding:12px}
  .invoice-payment-values{grid-template-columns:repeat(3,minmax(0,1fr));padding:8px}
  .invoice-payment-values span{font-size:9px}
  .invoice-payment-values strong{font-size:11px}
  .actions-footer,.backup-buttons,.form-actions{flex-direction:column}
  .actions-footer>*,.backup-buttons>*,.form-actions>*{width:100%;box-sizing:border-box}
}

@media(max-width:430px){
  .family-tab-btn{font-size:11px;padding:9px 4px}
  .invoice-payment-actions{grid-template-columns:1fr 1fr}
  .card-credit-values{grid-template-columns:1fr 1fr}
}

@media print{
  body{background:#fff!important}
  .container{border:0;box-shadow:none}
}
